# Chapter 7 — Which State Actually Generates Functional Loss?

**Status:** G2 source/claim map fixed.  
**Primary owner:** `zuizui0223/eco-genetic-criticality`.  
**Embedded firewall:** TU-3.  
**Worldline:** Loss / Dynamic State.

## Chapter thesis

A complete simulator state can be future-sufficient under its declared Markov closure without being the minimal state for functional loss, while familiar ecological and genetic marginals can remain too coarse because they erase transition-relevant cross-layer alignment.

## Forbidden inference

\[
\boxed{
	ext{raw simulator detail or matching coarse eco-genetic marginals}
\not\Rightarrow
	ext{the correct loss-generating state}
}
\]

## Source-owned eco-genetic results

- mutation-conditioned interaction memory and bounded H1/H3 mechanism results inside the declared finite closure;
- fragmentation can reduce interaction, local effective size, and realized high-trait mass before demographic disappearance;
- fixed absolute \(H_\alpha/H_\gamma\) thresholds are not robust canonical warning rules;
- complete explicit present state plus forcing/stochastic law is future-sufficient in the declared simulator;
- an aligned/anti-aligned constructive pair preserves census, interaction and allele-frequency marginals, realized trait-bin state, \(H_\alpha\), \(H_\gamma\), and \(F_{ST}\), yet changes the exact next interaction transition;
- the long-horizon campaign did not establish a universal directional alignment-risk rule.

## Embedded TU-3 role

TU-3 formalizes the representation firewall:

- a loss-faithful projection preserves the loss quotient;
- nuisance coordinates can inflate raw simulator state without increasing loss-state complexity;
- one hidden loss-relevant coordinate breaks the projection;
- `CompleteSimulatorState` and `LossGeneratingState` are not equal without a target-complete factorization and minimality result.

TU-3 is not presented as a new generic quotient or lumpability theorem.

## Planned sections

1. Interaction-supported function and fragmentation before demographic loss.
2. Complete simulator state under the declared closure.
3. Why common marginals are plausible but potentially unsafe summaries.
4. Aligned versus anti-aligned counterexample.
5. Transition-level conclusion and long-horizon non-claim.
6. Loss-response signature and TU-3 representation faithfulness.
7. Warning-blind freezing of the loss domain.
8. Handoff contract to warning evaluation.

## Main displays

- **Figure 7.1:** demographic, interaction, trait, genetic, and function layers.
- **Figure 7.2:** aligned versus anti-aligned patchwise joint states with shared marginals.
- **Table 7.1:** quantities held fixed and the next-transition quantities that differ.
- **Figure 7.3:** CompleteSimulatorState → loss-response signature → LossGeneratingState.

## Prohibited upgrades

- Cross-layer alignment universally increases or decreases long-horizon loss risk.
- The complete simulator state is the natural or minimal ecological state.
- Standard diversity and differentiation statistics are generally useless.
- A bounded simulator counterexample proves natural-system state insufficiency.
- A warning statistic may be used to select the loss-generating state.

## Hard dependency and handoff

This chapter must close the warning-blind loss-domain contract before Chapter 8. The transition paragraph should state exactly which simulator version, source conditions, endpoints, denominators, and state-selection fields are frozen, and which warning fields were excluded from that selection.

## Next drafting action

Write the aligned/anti-aligned result as the chapter's representation climax, then add a concise warning-blind state-lock section that Chapter 8 can cite without reselecting the domain.
